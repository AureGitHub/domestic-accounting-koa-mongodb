

<html>
    <head>
    </head>
    <body>

        <% if(NoFound === true) {%>
            <h2>No se ha encotrado el tipo de gasto (mal id)</h2>
        <% } %>

        <form action="/tipogasto/update" method="POST">
        <input type="hidden" name="_method" value="PUT">
            <h1>Tipo de Gasto</h1>
            <label>
            _id
            <%= item._id %>            
            </label>
            <br/>
            <input type="text" name="id" placeholder="descripcion" value="<%= item._id %>" visible="false">
            <br/>
            <label>
            descripcion
            <input type="text" name="descripcion" placeholder="descripcion" value="<%= item.descripcion %>" required/></br>
            </label>           
             <% if(!NoFound === true) {%>
                <input type="submit" value="Save"/>
            <% } %>
            
        </form>
    </body>
</html>
